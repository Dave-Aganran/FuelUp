{
  "name": "fuelup-poc",
  "version": "0.1.0",
  "private": true,
  "description": "FuelUp marketplace POC for downstream oil and gas outlets and buyers.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:init": "node src/db/init.js",
    "db:migrate": "node src/db/migrate.js",
    "check": "node --check src/server.js && node --check src/views.js && node --check src/data/memoryStore.js && node --check src/data/postgresStore.js && node --check src/validation.js && node --check src/config.js && node --check src/auth.js && node --check src/logger.js && node --check src/notifications.js && node --check src/payments/paystack.js && node --check src/payments/demo.js && node --check src/db/migrate.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^7.1.0",
    "pg": "^8.12.0"
  }
}
